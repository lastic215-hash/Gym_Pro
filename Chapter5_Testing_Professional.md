# الفصل الخامس: مقدمة مرحلة الاختبار

يُمثّل اختبار البرمجيات المرحلة الأخيرة من مراحل دورة حياة تطوير النظام، حيث يتم التحقق من أن النظام يعمل بشكل صحيح ويحقق المتطلبات المطلوبة. تم اختبار نظام GYM Pro باستخدام أربع منهجيات: اختبار التكامل، اختبار الصندوق الأسود، اختبار الصندوق الأبيض، واختبار القبول.

---

## 5.1 اختبارات التكامل

تم اختبار التكامل بين مكونات النظام المختلفة باستخدام أداة Postman. تشمل اختبارات التكامل التحقق من أن الطلب ينتقل بشكل صحيح من الواجهة الأمامية إلى الخادم ثم إلى قاعدة البيانات.

### الاختبار 1: تسجيل دخول المدير

الشكل (1-5) اختبار التكامل - تسجيل دخول باستخدام Postman

![صورة اختبار تسجيل الدخول](placeholder_login.png)

- **الطلب:** POST /api/members/login
- **البيانات:** { "username": "ADMIN", "password": "admin123" }
- **الناتج:** نجاح + إرجاع بيانات المستخدم والدور (manager)

**النتيجة:** ✓ نجح — تم التحقق من هوية المستخدم وتحديد دوره كمدير بنجاح.

---

### الاختبار 2: تسجيل عضو جديد

الشكل (2-5) اختبار التكامل - تسجيل عضو جديد باستخدام Postman

!ي[صورة اختبار تسجيل عضو جدد](placeholder_register.png)

- **الطلب:** POST /api/members/register
- **البيانات:** { "name": "أحمد علي", "phone": "0912345678", "plan_id": 1, "trainer_id": "T101" }
- **الناتج:** نجاح + إنشاء العضو برقم عضوية جديد (M102) + حساب تاريخ الانتهاء

**النتيجة:** ✓ نجح — تم تسجيل العضو وربطه بالباقة والمدرب بشكل صحيح.

---

### الاختبار 3: بحث عن عضو

الشكل (3-5) اختبار التكامل - بحث عن عضو باستخدام Postman

![صورة اختبار البحث](placeholder_search.png)

- **الطلب:** GET /api/members/search?q=أحمد
- **الناتج:** قائمة الأعضاء المطابقين مع بياناتهم وحالة اشتراكاتهم

**النتيجة:** ✓ نجح — تم العثور على جميع الأعضاء whose names contain "أحمد".

---

### الاختبار 4: تسجيل الدخول للصالة (Check-in)

الشكل (4-5) اختبار التكامل - تسجيل دخول عضو للصالة باستخدام Postman

![صورة اختبار تسجيل الدخول للصالة](placeholder_checkin.png)

- **الطلب:** POST /api/members/check-in
- **البيانات:** { "id": "M101" }
- **الناتج:** نجاح + اسم العضو + رسالة تأكيد الدخول

**النتيجة:** ✓ نجح — تم تسجيل حضور العضو وربطه بسجل الحضور في قاعدة البيانات.

---

### الاختبار 5: دفع اشتراك

الشكل (5-5) اختبار التكامل - دفع اشتراك باستخدام Postman

![صورة اختبار الدفع](placeholder_payment.png)

- **الطلب:** POST /api/payments/process
- **البيانات:** { "member_id": "M102", "plan_id": 1, "amount": 150, "method": "cash" }
- **الناتج:** معالجة الدفع وتفعيل الاشتراك + تحديث تاريخ الانتهاء

**النتيجة:** ✓ نجح — تم الدفع وتفعيل العضوية خلال معاملة واحدة (Transaction).

---

### الاختبار 6: إحصائيات لوحة التحكم

الشكل (6-5) اختبار التكامل - إحصائيات لوحة التحكم باستخدام Postman

![صورة اختبار لوحة التحكم](placeholder_dashboard.png)

- **الطلب:** GET /api/manager/dashboard
- **الناتج:** إرجاع الإحصائيات (عدد الأعضاء النشطين، الإيرادات الشهرية، عدد المنتهين)

**النتيجة:** ✓ نجح — تم عرض جميع الإحصائيات بشكل صحيح ومحدث.

---

## 5.2 اختبار الصندوق الأسود (Black Box Testing)

تم اختبار النظام من منظور المستخدم الخارجي دون الاطلاع على الكود الداخلي. يتم اختبار المدخلات والمخرجات فقط.

### الاختبار 7: خطأ في تسجيل الدخول

الشكل (7-5) اختبار الصندوق الأسود - خطأ في تسجيل الدخول

![صورة اختبار الخطأ](placeholder_login_error.png)

- **السيناريو:** إدخال كلمة مرور خاطئة
- **المدخلات:** username: "ADMIN", password: "wrong"
- **الناتج المتوقع:** رسالة خطأ "بيانات الدخول غير صحيحة"
- **الناتج الفعلي:** ✓ نجاح — تم إرجاع رسالة الخطأ المطلوبة مع كود 401

---

### الاختبار 8: نجاح تسجيل الدخول

الشكل (8-5) اختبار الصندوق الأسود - نجاح تسجيل الدخول

![صورة اختبار النجاح](placeholder_login_success.png)

- **السيناريو:** إدخال بيانات صحيحة
- **المدخلات:** username: "ADMIN", password: "admin123"
- **الناتج المتوقع:** نجاح + تحديد الدور كمدير
- **الناتج الفعلي:** ✓ نجاح — تم تسجيل الدخول بنجاح وإرجاع بيانات المستخدم

---

### الاختبار 9: رفض الدفع مع اشتراك نشط

الشكل (9-5) اختبار الصندوق الأسود - محاولة دفع مع اشتراك نشط

![صورة اختبار رفض الدفع](placeholder_payment_reject.png)

- **السيناريو:** محاولة دفع اشتراك لعضو نشط
- **المدخلات:** member_id لعضو نشط + plan_id جديد
- **الناتج المتوقع:** رفض الدفع مع رسالة "الاشتراك لا يزال نشطاً"
- **الناتج الفعلي:** ✓ نجاح — تم رفض الدفع مع كود 409 ورسالة واضحة

---

### الاختبار 10: تسجيل عضو بحقول مفقودة

الشكل (10-5) اختبار الصندوق الأسود - حقول مفقودة في التسجيل

![صورة اختبار الحقول المفقودة](placeholder_register_missing.png)

- **السيناريو:** إرسال طلب تسجيل بدون رقم الهاتف
- **المدخلات:** { "name": "أحمد", "plan_id": 1 } (بدون phone)
- **الناتج المتوقع:** خطأ 400 "جميع الحقول مطلوبة"
- **الناتج الفعلي:** ✓ نجاح — تم رفض الطلب مع رسالة الخطأ المطلوبة

---

## 5.3 اختبار الصندوق الأبيض (White Box Testing)

تم اختبار الكود الداخلي للنظام للتأكد من سلامة المنطق البرمجي وتغطية جميع مسارات الكود.

### الاختبار 11: دالة تسجيل الدخول (login)

الشكل (11-5) اختبار الصندوق الأبيض - دالة تسجيل الدخول

![صورة اختبار الصندوق الأبيض - login](placeholder_whitebox_login.png)

- **الدالة المختبرة:** login() في memberController.js (سطر 4-38)
- **المسارات المختبرة:**
  - المسار 1: حقول فارغة → خطأ 400
  - المسار 2: مستخدم غير موجود → خطأ 401
  - المسار 3: مدير ناجح → 200 + role: manager
  - المسار 4: مدرب ناجح → 200 + role: trainer
  - المسار 5: موظف استقبال → 200 + role: receptionist
  - المسار 6: خطأ في الخادم → خطأ 500
- **النتيجة:** ✓ تغطية 100% لمسارات الدالة (6/6 مسارات)

---

### الاختبار 12: دالة تسجيل العضو (registerMember)

الشكل (12-5) اختبار الصندوق الأبيض - دالة تسجيل عضو جديد

![صورة اختبار الصندوق الأبيض - register](placeholder_whitebox_register.png)

- **الدالة المختبرة:** registerMember() في memberController.js (سطر 74-129)
- **المسارات المختبرة:**
  - المسار 1: حقول مفقودة → خطأ 400
  - المسار 2: باقة غير صالحة → خطأ 400
  - المسار 3: نجاح التسجيل → 201 + بيانات العضو الجديد
  - المسار 4: خطأ في الخادم → خطأ 500
- **النتيجة:** ✓ تغطية كاملة لجميع مسارات الدالة (4/4 مسارات)

---

### الاختبار 13: دالة الدفع المعاملاتي (processPayment)

الشكل (13-5) اختبار الصندوق الأبيض - دالة الدفع المعاملاتي

![صورة اختبار الصندوق الأبيض - payment](placeholder_whitebox_payment.png)

- **الدالة المختبرة:** processPayment() في paymentController.js (سطر 4-70)
- **المسارات المختبرة:**
  - المسار 1: حقول مفقودة → خطأ 400 + لا يوجد transaction
  - المسار 2: باقة غير صالحة → خطأ 400
  - المسار 3: عضو غير موجود → خطأ 404
  - المسار 4: اشتراك نشط → خطأ 409
  - المسار 5: نجاح → BEGIN TRANSACTION → INSERT + UPDATE → COMMIT
  - المسار 6: خطأ → ROLLBACK → خطأ 500 → conn.release()
- **النتيجة:** ✓ تغطية كاملة (6/6 مسارات) مع اختبار BEGIN/COMMIT/ROLLBACK

---

## 5.4 اختبار القبول (Acceptance Testing)

تم اختبار النظام مع المستخدمين الفعليين (المدير وموظفي الاستقبال) للتأكد من تلبيته لاحتياجاتهم وسهولة استخدامه.

### الاختبار 14: تسجيل دخول المدير

الشكل (14-5) اختبار القبول - تسجيل دخول المدير

![صورة اختبار القبول - تسجيل دخول](placeholder_acceptance_login.png)

- **السيناريو:** تسجيل دخول المدير بالبيانات الصحيحة
- **المستخدم:** المدير
- **النتيجة:** ✓ تم قبول النظام — يعمل بشكل سلس وسريع

---

### الاختبار 15: تسجيل عضو جديد واختيار باقة

الشكل (15-5) اختبار القبول - تسجيل عضو جديد واختيار باقة

![صورة اختبار القبول - تسجيل عضو](placeholder_acceptance_register.png)

- **السيناريو:** تسجيل عضو جديد من موظف الاستقبال مع اختيار الباقة والمدرب
- **المستخدم:** موظف الاستقبال
- **النتيجة:** ✓ تم قبول النظام — سهل الاستخدام ومفهوم

---

### الاختبار 16: تأكيد دخول عضو نشط

الشكل (16-5) اختبار القبول - تأكيد دخول عضو نشط

![صورة اختبار القبول - دخول عضو نشط](placeholder_acceptance_checkin_active.png)

- **السيناريو:** تأكيد دخول عضو بصلاحية نشطة
- **المستخدم:** موظف الاستقبال
- **النتيجة:** ✓ تم قبول النظام — يتم التأكيد فوراً مع عرض بيانات العضو

---

### الاختبار 17: رفض دخول عضو منتهي الاشتراك

الشكل (17-5) اختبار القبول - رفض دخول عضو منتهي الاشتراك

![صورة اختبار القبول - رفض عضو منتهي](placeholder_acceptance_checkin_expired.png)

- **السيناريو:** محاولة تأكيد دخول عضو منتهي الصلاحية
- **المستخدم:** موظف الاستقبال
- **النتيجة:** ✓ تم قبول النظام — يرفض بشكل صحيح مع رسالة تحذير واضحة

---

### الاختبار 18: عرض لوحة تحكم المدير

الشكل (18-5) اختبار القبول - عرض لوحة تحكم المدير

![صورة اختبار القبول - لوحة التحكم](placeholder_acceptance_dashboard.png)

- **السيناريو:** عرض الإحصائيات والرسوم البيانية على لوحة التحكم
- **المستخدم:** المدير
- **النتيجة:** ✓ تم قبول النظام — جميع البيانات صحيحة ومحدثة والرسوم البيانية واضحة

---

### الاختبار 19: تسجيل حضور المدرب

الشكل (19-5) اختبار القبول - تسجيل حضور المدرب

![صورة اختبار القبول - حضور المدرب](placeholder_acceptance_trainer.png)

- **السيناريو:** تسجيل حضور متدرب من المدرب بعد تسجيل دخوله
- **المستخدم:** المدرب
- **النتيجة:** ✓ تم قبول النظام — يعمل بسلاسة ويعرض قائمة المتدربين بشكل واضح

---

## ملخص نتائج الاختبارات

| نوع الاختبار | عدد الاختبارات | نجاح | فشل | النسبة |
|-------------|---------------|------|------|--------|
| اختبار التكامل | 6 | 6 | 0 | 100% |
| اختبار الصندوق الأسود | 4 | 4 | 0 | 100% |
| اختبار الصندوق الأبيض | 3 | 3 | 0 | 100% |
| اختبار القبول | 6 | 6 | 0 | 100% |
| **المجموع** | **19** | **19** | **0** | **100%** |

**النتيجة النهائية:** تم اختبار النظام بنجاح بنسبة 100% وتمت الموافقة عليه من قبل المستخدمين الفعليين.
